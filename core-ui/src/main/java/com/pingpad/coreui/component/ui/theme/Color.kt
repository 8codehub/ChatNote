package com.pingpad.coreui.component.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors
val LightPrimary = Color(0xFF007AFF)//done // Use for primary actions (e.g., active buttons, chat bubbles).
val LightOnPrimary = Color(0xFFFFFFFF) // done // Ensure high contrast for onPrimary.
val LightSecondary = Color(0xFFC2C6CC) // done
val LightOnSecondary = Color(0xFF8E8E93) // done
val LightBackground = Color(0xFFFFFFFF)//done //Background should be the dominant canvas color.
val LightOnBackground = Color(0xFF000000)//done //Ensure OnBackground text is legible for key content.
val LightError = Color.Red


// Dark Theme Colors
val DarkPrimary = Color(0xFF19A3FE)//done
val DarkOnPrimary = Color(0xFFFFFFFF) // done
val DarkSecondary = Color(0xFF43474D) // done
val DarkOnSecondary = Color(0xFF8E8E93) // done
val DarkBackground = Color(0xFF000000) //done
val DarkOnBackground = Color(0xFFFFFFFF) // done
val DarkError = Color.Red // done