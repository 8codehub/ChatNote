package com.pingpad.coreui.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors
val LightPrimary = Color(0xFF007AFF) // Primary actions like active buttons and chat bubbles
val LightOnPrimary = Color(0xFFFFFFFF) // High contrast for content on primary
val LightSecondary = Color(0xFF8E8E93) // Secondary elements
val LightOnSecondary = Color(0xFFF2F2F2) // High contrast for content on secondary
val LightBackground = Color(0xFFFFFFFF) // Dominant canvas color
val LightOnBackground = Color(0xFF000000) // Key content legibility
val LightError = Color.Red // Error indicators

// Dark Theme Colors
val DarkPrimary = Color(0xFF19A3FE) // Primary actions in dark mode
val DarkOnPrimary = Color(0xFFFFFFFF) // High contrast for content on primary
val DarkSecondary = Color(0xFF8E8E93) // Secondary elements in dark mode
val DarkOnSecondary = Color(0xFFF2F2F2) // High contrast for content on secondary
val DarkBackground = Color(0xFF000000) // Dark mode background
val DarkOnBackground = Color(0xFFFFFFFF) // Key content legibility in dark mode
val DarkError = Color.Red // Error indicators in dark mode
